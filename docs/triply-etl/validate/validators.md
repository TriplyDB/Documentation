```ts
| Validator            | Functionality                                                                         |
| -------------------- | ------------------------------------------------------------------------------------- |
| `equals`             | Check if the string contains the seed.                                                |
| `isAfter`            | Check if the string is a date that is after the specified date.                       |
| `isAlpha`            | Check if the string contains only letters (a-zA-Z).                                   |
| `isAlphanumeric`     | Check if the string contains only letters and numbers (a-zA-Z0-9).                    |
| `isAscii`            | Check if the string contains ASCII chars only.                                        |
| `isBase32`           | Check if the string is a valid Base32-encoded string.                                 |
| `isBase58`           | Check if the string is a valid Base58-encoded string.                                 |
| `isBase64`           | Check if the string is a valid Base64-encoded string.                                 |
| `isBefore`           | Check if the string is a date that is before the specified date.                      |
| `isIBAN`             | Check if the string is a valid International Bank Account Number (IBAN).              |
| `isBIC`              | Check if the string is a valid Bank Identifier Code (BIC).                            |
| `isBoolean`          | Check if the string is a valid boolean.                                               |
| `isByteLength`       | Check if the byte length of the string is within a specified range.                   |
| `isCreditCard`       | Check if the string is a valid credit card number.                                    | 
| `isCurrency`         | Check if the string is a valid currency amount.                                       |
| `isDataURI`          | Check if the string is a valid Data URI.                                              |
| `isDate`             | Check if the string is a valid date.                                                  |
| `isDecimal`          | Check if the string is a valid decimal number.                                        |
| `isFloat`            | Check if the string is a valid floating-point number.                                 |
| `isFQDN`             | Check if the string is a valid Fully Qualified Domain Name (FQDN).                    |
| `isFullWidth`        | Check if the string contains any full-width chars.                                    |
| `isHalfWidth`        | Check if the string contains any half-width chars.                                    |
| `isHash`             | Check if the string is a valid hash (e.g., MD5, SHA-256).                             |
| `isHexadecimal`      | Check if the string is a valid hexadecimal number.                                    |
| `isHexColor`         | Check if the string is a valid hexadecimal color code.                                |
| `isHSL`              | Check if the string is a valid HSL (Hue, Saturation, Lightness) color code.           | 
| `isRgbColor`         | Check if the string is a valid RGB color code.                                        |
| `isIMEI`             | Check if the string is a valid International Mobile Equipment Identity (IMEI) number. |
| `isIn`               | Check if the string is one of the specified values.                                   |
| `isInt`              | Check if the string is a valid integer.                                               |
| `isIP`               | Check if the string is a valid IP address.                                            |
| `isIPRange`          | Check if the string is a valid IP range.                                              | 
| `isEAN`              | Check if the string is a valid International Article Number (EAN).                    |
| `isISIN`             | Check if the string is a valid International Securities Identification Number (ISIN). |
| `isISO31661Alpha2`   | Check if the string is a valid ISO 3166-1 alpha-2 country code.                       |
| `isISO31661Alpha3`   | Check if the string is a valid ISO 3166-1 alpha-3 country code.                       |
| `isISO6346`          | Check if the string is a valid ISO 6346 shipping container code.                      |
| `isISO6391`          | Check if the string is a valid ISO 639-1 language code.                               |
| `isISO8601`          | Check if the string is a valid ISO 8601 date.                                         |
| `isISSN`             | Check if the string is a valid International Standard Serial Number (ISSN).           |
| `isISO4217`          | Check if the string is a valid ISO 4217 currency code.                                |
| `isISRC`             | Check if the string is a valid International Standard Recording Code (ISRC).          |
| `isRFC3339`          | Check if the string is a valid RFC 3339 date.                                         |
| `isJSON`             | Check if the string is a valid JSON string.                                           |
| `isJWT`              | Check if the string is a valid JSON Web Token (JWT).                                  |
| `isLatLong`          | Check if the string is a valid latitude and longitude.                                |
| `isLength`           | Check if the length of the string is within a specified range.                        |
| `isLocale`           | Check if the string is a valid locale.                                                |
| `isLowercase`        | Check if the string contains only lowercase letters.                                  |
| `isUppercase`        | Check if the string contains only uppercase letters.                                  |
| `isMimeType`         | Check if the string is a valid MIME type.                                             |
| `isMobilePhone`      | Check if the string is a valid mobile phone number.                                   | 
| `isMongoId`          | Check if the string is a valid MongoDB ObjectId.                                      |
| `isMultibyte`        | Check if the string contains one or more multibyte chars.                             |
| `isNumeric`          | Check if the string is a valid numeric value.                                         |
| `isOctal`            | Check if the string is a valid octal number.                                          |
| `isPassportNumber`   | Check if the string is a valid passport number.                                       |
| `isPort`             | Check if the string is a valid port number.                                           |
| `isPostalCode`       | Check if the string is a valid postal code.                                           |
| `isSemVer`           | Check if the string is a valid Semantic Version (SemVer).                             |
| `isStrongPassword`   | Check if the string is a strong password.                                             |
| `isSurrogatePair`    | Check if the string contains any surrogate pairs chars.                               |
| `isTime`             | Check if the string is a valid time.                                                  |
| `isURL`              | Check if the string is a valid URL.                                                   |
| `isTaxID`            | Check if the string is a valid tax ID.                                                |
| `isUUID`             | Check if the string is a valid UUID.                                                  |
| `isVAT`              | Check if the string is a valid Value Added Tax (VAT) number.                          |
| `isWhitelisted`      | Check if the string contains only whitelisted characters.                             |
| `matches`            | Check if the string matches the specified pattern.                                    |
| `isSlug`             | Check if the string is of type slug.                                                  |
```